/**
 * Base prompt for image generation
 * Update this value directly when you need to change the prompt
 */
export const BASE_PROMPT =
  "Square surreal oil painting in a baroque gold frame, dark museum lighting. Depict the current state of the world.";

/**
 * Negative prompt for image generation
 * Update this value directly when you need to change the negative prompt
 */
export const NEGATIVE_PROMPT = "low quality, bad anatomy, disfigured, text artifacts";
